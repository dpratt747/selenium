{
  "name": "mvp_test",
  "version": "0.0.1",
  "description": "automated e2e tests for mvp",
  "main": "index.js",
  "scripts": {
    "build": "npm install -g selenium-server chromedriver | npm install",
    "clean": "rm -rf node_modules | rm package-lock.json | npm uninstall -g selenium-server chromedriver",
    "test": "mocha --no-timeouts --recursive"
  },
  "author": "davidpratt",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.2.0",
    "chromedriver": "^2.43.3",
    "geckodriver": "^1.14.1",
    "iedriver": "^3.9.2",
    "mocha": "^5.2.0",
    "selenium-webdriver": "^4.0.0-alpha.1",
    "webdriverio": "^4.14.0"
  }
}
